# 通信知识体系 — 项目配置

## 项目概述

本文件夹是 **通信系统** 的知识体系仓库，覆盖信号与系统基础、通信原理、无线通信。以三本经典教材为学习路径，建立从数学基础到系统架构的贯通知识索引。

**核心理念**：
- 知识遵循从数学基础 → 通信原理 → 无线通信的自然递进
- 信号与系统是数学语言，通信原理是系统框架，无线通信是专项深入
- 笔记各自保持独立的叙事风格，但通过 `[[知识导图]]` 索引中枢和 wikilink 互联
- 概念本体（Markmap 思维导图）稳定，阅读路径（子文件夹）随学习扩展

## 文件夹结构

```
Communication/
├── CLAUDE.md                                    # 本文件：框架级配置
├── 知识导图.md                                   # 概念本体索引中枢（Markmap 思维导图 + L4 wikilink）
│
├── Oppenheim-信号与系统/                          # [子文件夹] 信号与系统教材笔记（第二次学习）
├── 通信原理/                                     # [子文件夹] 通信原理教材笔记（首次学习）
└── Goldsmith-无线通信/                           # [子文件夹] 无线通信教材笔记（首次学习）
```

**规则**：
- 每本教材一个子文件夹
- 子文件夹命名：`作者-书名简称`（无作者的教材直接用书名）
- 笔记内部使用 `[[子文件夹名/文件名#标题]]` 跨文件夹引用

---

## 知识导图 (Markmap) 索引体系

### 架构

本项目的概念索引由 `[[知识导图]]` 一文承担，采用 **纯 Markdown 标题层级 = 思维导图** 的方案（通过 `obsidian-markmind` 插件渲染为 Markmap）。

语法即结构，无需额外格式：

| 层级 | Markdown | 含义 | 示例 |
|------|----------|------|------|
| L0 | `#` | 根节点 | `# 通信知识体系` |
| L1 | `##` | 知识域 | `## 信号与系统基础` |
| L2 | `###` | 子域/章 | `### 采样` |
| L3 | `####` | 具体概念 | `#### 采样定理` |
| L4 | `- [[wikilink]]` | 笔记链接 | `- [[Oppenheim-信号与系统/采样#Nyquist\|Oppenheim: 采样定理]]` |

**Markmap 方案的优势**：
- **零语法**：增删概念就是增删 markdown 标题，不需要学任何 DSL
- **零坐标**：自动树形布局，永远不重叠
- **可点击**：`- [[wikilink]]` 列表项在思维导图中直接可点击跳转
- **可折叠**：节点多了可以折叠子树，不像 Canvas/Mermaid 那样撑爆视野
- **跨域关联**：通过列表项内的 `→` `←` `↔` `⚡` 等标记 + wikilink 实现

### 跨域关联的标注约定

Markmap 是树结构，不原生支持图边。跨域关系通过 L3 概念下的列表项标注：

```
#### 傅里叶变换
- [[Oppenheim-信号与系统/连续时间傅里叶变换#定义\|Oppenheim: CTFT 定义]]
- ⚡ 时域-频域对偶：窄时域 ↔ 宽频域
- → 应用：[[通信原理/模拟调制系统#DSB-SC\|调制中的频谱搬移]]
```

常用标记：
- `→` / `←`：单向依赖/应用（"A 用于 B"）
- `↔`：双向关联（"A 与 B 互补"）
- `⚡`：关键 trade-off / 设计洞察

### 知识导图的自动维护规则

**触发时机**：AI 每次完成一篇笔记的撰写或修改后，**必须**执行以下维护：

1. **读取 `[[知识导图]]`**：了解已有概念树结构
2. **增量更新标题层级**：
   - 若笔记引入了新概念 → 在对应 L2 子域下添加 `#### 概念名` + L4 链接列表项
   - 若新概念属于已有 L3 → 在已有 `####` 下添加新的 L4 `- [[link]]`
   - 若新概念需要新建 L2 子域甚至 L1 知识域 → 先与用户确认
3. **添加跨域关联标注**：
   - 若新概念与已有概念存在跨域关系 → 在两端概念下各加一行 `→` / `↔` 标注
4. **添加新教材的 L4 链接**：
   - 同一概念多本书时，在对应 `####` 下追加 `- [[新书子文件夹/笔记#标题\|新书: 概念名]]`
   - 可附一句标注各书视角差异

**不做的操作**：
- ❌ 不删除已有概念/链接（除非知识归类明显错误且用户确认）
- ❌ 不为每个 `###` 子标题创建 `####` 概念节点（只到笔记的 `#` / `##` 级别）
- ❌ 不为尚未写的章节预建节点

---

## 笔记撰写通用标准

以下规则适用于所有教材的笔记，不受具体教材风格影响。

### 深度原则

- **深度而非堆砌**：宁可一个知识点写透，也不要罗列十个概念定义
- **联想与贯通**：重在建立前后章节的联系，融汇贯通
- **直觉与数学并重**：每个结论既要物理直觉的直观分析，也要数学表达式的定量理解

### 组织原则

按照知识发展的自然顺序组织笔记——概念从哪里来 → 它是什么 → 它为什么重要 → 它通向哪里。物理直觉、数学推导、设计启示在知识展开中自然融入。

- 物理直觉和数学公式穿插使用，哪个适合解释当前问题就用哪个
- 概念定义、推导逻辑、设计意义之间平滑过渡
- 需要深入展开的知识点用 `###` 子标题自然引出

### 语言风格：简明扼要

**核心原则：结论直接说，不铺陈；保留所有技术实质，砍掉一切文字水分。**

- 不要写过渡句和引导句——"一个容易忽略的事实是""值得深思的是"这类句子直接删掉
- 同一个观点只解释一遍——在第一遍解释时就找准角度，一次说透
- 删掉过度修饰——让事实本身说话
- 不开辟大段背景铺陈——直接进入主题，必要的上下文一句话交代即可
- 允许写短段落，甚至一句话成段——节奏感比"完整性"重要
- 公式前后的引导句可以极简——"由此可得""即"

### 标题层级规则

- `#` — 章级小节（对应原书的一级 numbered section）
- `##` — 子节（对应原书的二级 numbered section），或章级小节内的主要子主题
- `###` — 子节内的具体知识点/例题，用于自然切分阅读节奏，不做更深的层级嵌套

### 数学推导准则

1. 给出起点和终点公式
2. 中间步骤写明用到的思想方法和近似，不展开全部代数运算
3. 关键步骤适当展开（如未做近似的原始结论、重要的中间结果）
4. 对结果公式逐项解读物理含义

### 术语规范

- 重要术语首次出现时，中文 + 英文/符号并列：`跨导（transconductance, $g_m$）`
- 后续统一使用符号或最常用称呼
- 公式中的符号遵循对应原书约定

### 插图（教材笔记专用）

教材插图**直接嵌入**笔记对应位置，不再使用 `> 【插入 … 图 X.XX】` 占位标注。

流程：
1. 从教材 `images/` 目录定位对应图片文件
2. 将该图片**复制**至 `E:\base_Obsidian\iceaxing's knowledge base\System\Attachments\`（文件名若为无意义哈希串，可重命名为 `书名-章节-图号.jpg` 之类的可读名），**原教材 `images/` 目录内容保持不变**
3. 在笔记对应位置用 Obsidian 嵌入语法插入：`![[图片文件名.jpg]]`，可附 `|宽度px` 控制显示尺寸

规则：
- 同一张图只复制一次，避免重复占用
- 图可配一句图注说明内容，图片本身直接嵌入正文

### AI 行为准则

#### 对每个结论，追问两个问题

1. **为什么（Why）**：物理直觉上为什么会这样？数学上为什么必然如此？
2. **然后呢（So What）**：这个结论对通信系统设计意味着什么？它会在后续哪一章用到？

#### 联想与贯通

- 遇到与前面章节呼应的内容，使用 `[[子文件夹/章节名]]` 链接，并**用一句话点明联系**
- 主动标注知识点在整个知识体系中的位置（如"这是通信系统三大核心折中之一：带宽-功率-误码率"）
- 可以将结论与 vault 内其他领域关联：`[[IC/Razavi-RF Microelectronics/Transceiver Architecture]]`

#### 重要概念的首次引入

首次出现的重要概念必须给出：
- 中英双语名称
- 精确定义
- 物理含义
- 在通信系统设计中的意义

#### 写笔记前的上下文准备

AI 在撰写新章节笔记前，应执行以下步骤：
1. **读 `[[知识导图]]`**：在 Markmap 思维导图中定位该知识点在整体体系中的位置、与哪些已有概念关联
2. **读教材原文**：从对应章节的 markdown 文件读取原书内容
3. **搜相关笔记**：搜索其他子文件夹中是否有同一概念的讲解（用于跨书对比）
4. **在笔记中体现位置感**：利用 wikilink 链接到前置概念和后续应用

#### 不要做的事

- ❌ 不要逐句翻译教材段落
- ❌ 不要罗列无解释的公式
- ❌ 不要写"显然"、"易得"——把"为什么显然"说清楚
- ❌ 不要在不确定的情况下编造内容——宁可标注 `（此处需对照原书确认）`

### Obsidian 双向链接

本项目位于 Obsidian vault 内，使用 `[[wikilink]]` 链接。

**链接路径规则**：
- 同子文件夹内：`[[笔记文件名]]` 或 `[[笔记文件名#标题]]`
- 跨子文件夹：`[[子文件夹名/笔记文件名#标题]]`
- Vault 内其他领域：`[[IC/Razavi-RF Microelectronics/Transceiver Architecture|RFIC: 收发机架构]]`

链接的使用场景：
- 推导中引用之前的结论
- 指出某个概念将在后续章节展开
- 标注"初学易错点"的提示

---

## 教材插槽

### Oppenheim — 《Signals and Systems》（第2版）

**子文件夹**：`Oppenheim-信号与系统/`

**学习状态**：🔄 第二次学习（复习巩固 + 深化理解）

**笔记定位**：复习笔记——以回顾和深化为主，重点打通与通信原理、无线通信的数学桥梁。不再逐章逐节重述，而是聚焦关键概念的本质理解和跨域关联。

**教材位置**：
- **Windows**：`E:\_textbook-md\Signal and System\textbook\`
- **Linux**：`/home/iceaxing/MyDoc/_textbook-md/Signal and System/textbook/`

教材为单个 markdown 文件：
- `Signals_and_Systems_Oppenheim_2nd.md`

配套图片位于 `images/` 文件夹。

**教材特点**：Oppenheim 是信号与系统领域的圣经级教材，以清晰的数学框架和丰富的例题著称。第二次学习的重点是打通"数学工具"与"通信应用"之间的连接——傅里叶变换不是目的，调制/采样/滤波才是。

**章节范围**：第 1–11 章

**章节组织**：
- 每个章节一个 `.md` 文件，按用户规划的章节顺序命名
- 第二次学习可以合并相关章节（如傅里叶级数+傅里叶变换）
- 笔记常以原书 Figure 编号作为锚点，格式：`> 【插入 Oppenheim 图 X.XX】描述`

**覆盖范围**：
- ✅ 教材正文（重点复习）
- ✅ 关键例题
- ❌ 课后习题（目前不覆盖）

**当前进度**：尚未开始

---

### 通信原理（第7版）

**子文件夹**：`通信原理/`

**学习状态**：🔄 第二次学习（复习巩固 + 深化理解）

**笔记定位**：第 2 次学习 —— 以回顾和深化为主，不再逐章逐节重述，聚焦调制、编码、同步等链路环节的本质理解与跨域关联，重点打通与信号与系统、无线通信之间的桥梁。

**教材位置**：
- **Windows**：`E:\_textbook-md\通信原理\textbook-md\`
- **Linux**：`/home/iceaxing/MyDoc/_textbook-md/通信原理/textbook-md/`

教材为 10 个部分 + 1 个完整版：
- `通信原理_完整版.md`
- `part_01/` 至 `part_10/`

配套图片位于各 `part_*/images/` 文件夹。

**教材特点**：国内通信原理经典教材，覆盖模拟调制、数字基带/带通传输、信源编码、信道编码、同步等完整通信系统链路。首次学习需建立从发送端到接收端的完整系统视角。

**章节范围**：第 1–13 章 + 附录

**笔记风格**（首次学习特有）：
- 推导：关键步骤与思想方法写出，不展开全部代数运算
- 物理直觉与数学并重
- **初学特有**：每个新概念交代"为什么需要它"的背景（如：为什么需要调制？为什么需要同步？）
- **初学特有**：数值实例充分展开，给出物理量级感
- ❌ 不做逐句翻译，不罗列无解释的公式

**章节组织**：
- 每个章节一个 `.md` 文件，不严格遵循原书章节编号
- 章节顺序由用户逐步添加

**覆盖范围**：
- ✅ 教材正文
- ✅ 关键例题
- ❌ 课后习题（目前不覆盖）

**当前进度**：尚未开始

---

### Goldsmith — 《Wireless Communications》

**子文件夹**：`Goldsmith-无线通信/`

**学习状态**：🔄 第二次学习（复习巩固 + 深化理解）

**笔记定位**：第 2 次学习 —— 以回顾和深化为主，不再逐章逐节重述，聚焦无线信道独特挑战（衰落、多径、移动性）的本质理解与跨域关联，重点打通与通信原理之间的桥梁。

**教材位置**：
- **Windows**：`E:\_textbook-md\Wireless Communication\textbook-md\`
- **Linux**：`/home/iceaxing/MyDoc/_textbook-md/Wireless Communication/textbook-md/`

教材为单个 markdown 文件：
- `Wireless-Communications-Goldsmith.md`

配套图片位于 `images/` 文件夹。

**教材特点**：Goldsmith 是无线通信领域的经典教材，以信道模型为主线，逐步展开从物理层到网络层的完整无线通信体系。与通信原理教材形成互补——通信原理侧重"通信链路"，Goldsmith 侧重"无线信道"带来的独特设计挑战。

**章节范围**：第 1–16 章

**笔记风格**（与通信原理一致，差异仅在关注点）：
- 推导：关键步骤与思想方法写出，不展开全部代数运算
- 物理直觉与数学并重
- **初学特有**：每个新概念交代"为什么无线信道不同"（如：为什么有线信道的容量公式在无线中不够用？）
- **初学特有**：数值实例充分展开，与通信原理中的理想信道做对比
- ❌ 不做逐句翻译，不罗列无解释的公式

**章节组织**：
- 每个章节一个 `.md` 文件
- 章节顺序由用户逐步添加

**覆盖范围**：
- ✅ 教材正文
- ✅ 关键例题
- ❌ 课后习题（目前不覆盖）

**当前进度**：尚未开始
