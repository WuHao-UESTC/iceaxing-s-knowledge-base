# 数字设计知识体系 — 项目配置

## 项目概述

本文件夹是 **数字设计** 的知识体系仓库，覆盖数字逻辑设计、硬件描述语言、计算机体系结构与组成。以四本经典教材为学习路径，建立从逻辑门到高级体系结构的完整知识索引。

**核心理念**：
- 数字设计是一个连续谱：逻辑门 → 组合/时序电路 → 数据通路/控制器 → 处理器 → 多核/并行 → 领域专用架构
- 四本教材切在不同深度——Wakerly 偏重逻辑设计实践，Harris 以体系结构视角串起数字设计，Patterson（COD）以处理器为核心展开计算机组成，Hennessy（CAQA）将体系结构推向量化分析与高级主题
- 笔记各自保持独立的叙事风格，但通过 `[[知识导图]]` 索引中枢和 wikilink 互联
- 概念本体（Markmap 思维导图）稳定，阅读路径（子文件夹）随学习扩展

**四本书的定位与关系**：

| 教材 | 定位 | 学习次数 |
|------|------|----------|
| Wakerly — 数字设计原理与实践 | 数字逻辑设计根基：从 0/1 到状态机，强调设计实践与 Verilog | 第 2 次 |
| Harris — 数字设计和计算机体系结构 | 桥梁：以体系结构为目标倒推数字设计，最终构建 ARM 微处理器 | 第 2 次 |
| Patterson — 计算机组成与设计 | 组成与体系结构基础：ISA、流水线、存储层次、并行，RISC-V | 第 1 次 |
| Hennessy — 计算机体系结构（量化方法） | 体系结构高级纵深：ILP、DLP、WSC、DSA，量化设计方法论 | 第 1 次 |

**学习路径**：Wakerly（逻辑基础巩固）→ Harris（体系结构视角整合）→ Patterson COD（组成与设计基础）→ Hennessy CAQA（量化分析纵深）

## 文件夹结构

```
Digital Design/
├── CLAUDE.md                                    # 本文件：框架级配置
├── 知识导图.md                                   # 概念本体索引中枢（Markmap 思维导图 + L4 wikilink）
│
├── Wakerly-数字设计原理与实践/                    # [子文件夹] 数字逻辑设计教材笔记
│
├── Harris-数字设计和计算机体系结构/                # [子文件夹] 数字设计→体系结构桥梁教材笔记
│
├── Patterson-计算机组成与设计/                    # [子文件夹] 计算机组成基础教材笔记
│
├── Hennessy-计算机体系结构量化方法/               # [子文件夹] 体系结构高级纵深教材笔记
│
└── Papers/                                      # [子文件夹] 独立论文笔记
```

**规则**：
- 每本教材一个子文件夹
- 子文件夹命名：`作者-书名简称`
- 论文放入 `Papers/`，按 `作者-年份-主题.md` 命名
- 笔记内部使用 `[[子文件夹名/文件名#标题]]` 跨文件夹引用
- 与 vault 内其他知识库互引：`[[IC/...]]`、`[[IC/知识导图]]`（例如 CMOS 门电路 ↔ 晶体管级实现）

---

## 知识导图 (Markmap) 索引体系

### 架构

本项目的概念索引由 `[[知识导图]]` 一文承担，采用 **纯 Markdown 标题层级 = 思维导图** 的方案（通过 `obsidian-markmind` 插件渲染为 Markmap）。

语法即结构，无需额外格式：

| 层级 | Markdown | 含义 | 示例 |
|------|----------|------|------|
| L0 | `#` | 根节点 | `# 数字设计知识体系` |
| L1 | `##` | 知识域 | `## 组合逻辑设计` |
| L2 | `###` | 子域 | `### 常用组合电路模块` |
| L3 | `####` | 具体概念 | `#### 多路选择器` |
| L4 | `- [[wikilink]]` | 笔记链接 | `- [[子文件夹/笔记#标题\|Wakerly: 多路选择器]]` |

**Markmap 方案的优势**：
- **零语法**：增删概念就是增删 markdown 标题，不需要学任何 DSL
- **零坐标**：自动树形布局，永远不重叠
- **可点击**：`- [[wikilink]]` 列表项在思维导图中直接可点击跳转
- **可折叠**：节点多了可以折叠子树，不像 Canvas/Mermaid 那样撑爆视野
- **跨域关联**：通过列表项内的 `→` `←` `↔` `⚡` 等标记 + wikilink 实现

### 跨域关联的标注约定

Markmap 是树结构，不原生支持图边。跨域关系通过 L3 概念下的列表项标注：

```
#### 流水线冒险
- [[Patterson-计算机组成与设计/处理器#流水线冒险|Patterson: 流水线冒险]]
- ⚡ trade-off: 深流水 → 频率↑ 但 冒险惩罚↑
- ← 前提概念：[[#时序逻辑设计|状态机]]、[[#数据通路]]
```

常用标记：
- `→` / `←`：单向依赖/应用（"A 用于 B"）
- `↔`：双向关联（"A 与 B 互补"）
- `⚡`：关键 trade-off / 设计洞察

### 知识导图的自动维护规则

**触发时机**：AI 每次完成一篇笔记的撰写或修改后，**必须**执行以下维护：

1. **读取 `[[知识导图]]`**：了解已有概念树结构
2. **增量更新标题层级**：
   - 若笔记引入了新概念 → 在对应 L2 子域下添加 `#### 概念名` + L4 链接列表项
   - 若新概念属于已有 L3 → 在已有 `####` 下添加新的 L4 `- [[link]]`
   - 若新概念需要新建 L2 子域甚至 L1 知识域 → 先与用户确认
3. **添加跨域关联标注**：
   - 若新概念与已有概念存在跨域关系 → 在两端概念下各加一行 `→` / `↔` 标注
4. **添加新教材/论文的 L4 链接**：
   - 同一概念多本书时，在对应 `####` 下追加 `- [[新书子文件夹/笔记#标题\|新书: 概念名]]`
   - 可附一句标注各书视角差异

**不做的操作**：
- ❌ 不删除已有概念/链接（除非知识归类明显错误且用户确认）
- ❌ 不为每个 `###` 子标题创建 `####` 概念节点（只到笔记的 `#` / `##` 级别）
- ❌ 不为尚未写的章节预建节点

---

## 笔记撰写通用标准

以下规则适用于所有课本/论文的笔记，不受具体教材风格影响。

### 深度原则

- **深度而非堆砌**：宁可一个知识点写透，也不要罗列十个概念定义
- **联想与贯通**：重在建立前后章节的联系，融汇贯通
- **直觉与数学并重**：每个结论既要物理直觉（时序/状态/数据流），也要数学表达式（布尔方程、时序约束、性能公式）的定量理解

### 组织原则

按照知识发展的自然顺序组织笔记——概念从哪里来 → 它是什么 → 它为什么重要 → 它通向哪里。物理直觉、数学推导、设计启示在知识展开中自然融入。

- 物理直觉和数学公式穿插使用，哪个适合解释当前问题就用哪个
- 概念定义、推导逻辑、设计意义之间平滑过渡
- 需要深入展开的知识点用 `###` 子标题自然引出

### 语言风格：简明扼要

**核心原则：结论直接说，不铺陈；保留所有技术实质，砍掉一切文字水分。**

- 不要写过渡句和引导句——"一个容易忽略的事实是""值得深思的是"这类句子直接删掉
- 同一个观点只解释一遍——在第一遍解释时就找准角度，一次说透
- 删掉过度修饰——让事实本身说话
- 不开辟大段背景铺陈——直接进入主题，必要的上下文一句话交代即可
- 允许写短段落，甚至一句话成段——节奏感比"完整性"重要
- 公式前后的引导句可以极简——"由此可得""即"

### 标题层级规则

- `#` — 章级小节（对应原书的一级 numbered section）
- `##` — 子节（对应原书的二级 numbered section），或章级小节内的主要子主题
- `###` — 子节内的具体知识点/例题，用于自然切分阅读节奏，不做更深的层级嵌套

### 数学推导准则

1. 给出起点和终点公式
2. 中间步骤写明用到的思想方法和近似，不展开全部代数运算
3. 关键步骤适当展开（如未做近似的原始结论、重要的中间结果）
4. 对结果公式逐项解读物理含义

### 术语规范

- 重要术语首次出现时，中文 + 英文/符号并列：`建立时间（setup time, $t_{su}$）`
- 后续统一使用符号或最常用称呼
- 公式中的符号遵循对应原书约定

### 插图（教材笔记专用）

教材插图**直接嵌入**笔记对应位置，不再使用 `> 【插入 … 图 X.XX】` 占位标注。

流程：
1. 从教材 `images/` 目录定位对应图片文件
2. 将该图片**复制**至 `E:\base_Obsidian\iceaxing's knowledge base\System\Attachments\`（文件名若为无意义哈希串，可重命名为 `书名-章节-图号.jpg` 之类的可读名），**原教材 `images/` 目录内容保持不变**
3. 在笔记对应位置用 Obsidian 嵌入语法插入：`![[图片文件名.jpg]]`，可附 `|宽度px` 控制显示尺寸

规则：
- 同一张图只复制一次，避免重复占用
- 图可配一句图注说明内容，图片本身直接嵌入正文
### AI 行为准则

#### 对每个结论，追问两个问题

1. **为什么（Why）**：物理直觉上为什么会这样？数学上为什么必然如此？
2. **然后呢（So What）**：这个结论对数字系统设计意味着什么？它会在后续哪一章用到？

#### 联想与贯通

- 遇到与前面章节呼应的内容，使用 `[[子文件夹/章节名]]` 链接，并**用一句话点明联系**
- 主动标注知识点在整个知识体系中的位置（如"这是数字设计三大核心折中之一：速度-面积-功耗"）
- 可以将结论与 Digital Design 内其他子领域及 vault 内其他领域关联：`[[IC/Razavi-模拟CMOS集成电路设计/MOS器件物理基础]]`（CMOS 门电路的晶体管级基础）、`[[IC/...]]`

#### 重要概念的首次引入

首次出现的重要概念必须给出：
- 中英双语名称
- 精确定义
- 物理含义
- 在数字系统设计中的意义

#### 写笔记前的上下文准备

AI 在撰写新章节笔记前，应执行以下步骤：
1. **读 `[[知识导图]]`**：在 Markmap 思维导图中定位该知识点在整体体系中的位置、与哪些已有概念关联
2. **读教材原文**：从对应页数范围读取原书内容
3. **搜相关笔记**：搜索其他子文件夹中是否有同一概念的讲解（用于跨书对比）
4. **在笔记中体现位置感**：利用 wikilink 链接到前置概念和后续应用

#### 不要做的事

- ❌ 不要逐句翻译教材段落
- ❌ 不要罗列无解释的公式
- ❌ 不要写"显然"、"易得"——把"为什么显然"说清楚
- ❌ 不要在不确定的情况下编造内容——宁可标注 `（此处需对照原书 X 页确认）`

### Obsidian 双向链接

本项目位于 Obsidian vault 内，使用 `[[wikilink]]` 链接。

**链接路径规则**：
- 同子文件夹内：`[[笔记文件名]]` 或 `[[笔记文件名#标题]]`
- 跨子文件夹：`[[子文件夹名/笔记文件名#标题]]`
- Vault 内其他领域：`[[IC/Razavi-模拟CMOS集成电路设计/MOS器件物理基础|CMOS 门电路基础]]`

链接的使用场景：
- 推导中引用之前的结论
- 指出某个概念将在后续章节展开
- 标注"初学易错点"的提示

---

## 课本插槽

### Wakerly — 《数字设计原理与实践》（第五版·英文版，2018）

**子文件夹**：`Wakerly-数字设计原理与实践/`

**笔记定位**：第 2 次学习 —— 巩固数字逻辑设计根基，侧重设计实践与 Verilog。以物理直觉（信号时序、电路行为）为引导、数学工具（布尔代数、时序分析）为定量支撑。

**教材位置**：`E:\study_Digital Design\ai-reading\数字设计原理与实践\textbook-md\`

教材为单个 markdown 文件：
- `数字设计原理与实践第5版.md`

配套图片位于 `images/` 文件夹。

**教材特点**：Wakerly 以严谨的逻辑层级展开，从基本原理到设计实践，强调 "how to design"。覆盖数制编码、开关代数、组合/时序电路、Verilog、数字电路实现。是全套数字设计知识的根基。

**章节组织**：
- 每个章节一个 `.md` 文件，按**用户规划的章节顺序**命名
- 章节顺序由用户逐步添加，AI 不需要预先规划全部章节

**覆盖范围**：
- ✅ 教材正文
- ✅ 例题
- ❌ 课后习题（目前不覆盖）

**当前进度**：尚未开始

---

### Harris — 《数字设计和计算机体系结构》（原书第 2 版·ARM 版，2016）

**子文件夹**：`Harris-数字设计和计算机体系结构/`

**笔记定位**：第 2 次学习 —— 以体系结构为目标的数字设计整合。与 Wakerly 的知识点高度重叠但视角不同：Wakerly 从逻辑设计往上走，Harris 从体系结构往下看。

**教材位置**：`E:\study_Digital Design\ai-reading\数字设计和计算机体系结构\textbook-md\`

教材为单个 markdown 文件：
- `数字设计和计算机体系结构.md`

配套图片位于 `images/` 文件夹。

**教材特点**：Harris 采用独特的 "从 0/1 到 ARM 微处理器" 路径，将组合逻辑、时序逻辑、HDL、数字模块、体系结构和微体系结构串成一条线。ARM 版以 ARM 指令集为教学载体，并列讲解 SystemVerilog 和 VHDL。核心设计原则是 "层次化、模块化、规整化"。

**笔记风格**（与 Wakerly 互补，差异在视角）：
- 同一知识点避免重复 Wakerly 的展开方式，侧重从体系结构角度审视（如"这个组合电路模块在数据通路中扮演什么角色？"）
- 对 Harris 独有内容（微体系结构、ARM 指令集）按首次学习深度处理
- ❌ 不做逐句翻译，不罗列无解释的公式

**章节组织**：
- 不严格遵循原书章节编号，按用户规划命名
- 章节顺序由用户逐步添加

**覆盖范围**：
- ✅ 教材正文
- ✅ 例题
- ❌ 课后习题（目前不覆盖）

**当前进度**：尚未开始

---

### Patterson & Hennessy — 《计算机组成与设计：RISC-V 版》

**子文件夹**：`Patterson-计算机组成与设计/`

**笔记定位**：第 1 次学习 —— 需要更加注重概念的物理背景和设计动机。每个新概念需交代"为什么需要它"、给出数值量级感、充分展开例子。推导深度与通用标准一致：关键结论详细解释，推导过程仅写关键步骤与思想方法。

**教材位置**：`E:\study_Computer Organization and Design\textbook-md\`

教材为单个 markdown 文件：
- `Computer-Organization-and-Design-RISC-V-Edition.md`

配套图片位于 `images/` 文件夹。

**教材特点**：Patterson & Hennessy 是计算机体系结构领域的经典（"P&H"），以 RISC-V 指令集为载体，从程序员的视角逐步深入到处理器内部。覆盖 ISA、算术单元、数据通路与控制器、流水线、存储器层次结构、并行处理器。强调定量化设计原则（"Make the common case fast"）。

**笔记风格**：
- 推导：关键步骤与思想方法写出，不展开全部代数运算（同通用标准）
- 物理直觉与数学并重（同通用标准）
- **初学特有**：每个新概念交代"为什么需要它"的背景（如：为什么需要流水线？单周期处理器的瓶颈在哪？）
- **初学特有**：数值实例充分展开，给出物理量级感（如 CPI、IPC、Miss rate → AMAT 的计算）
- ❌ 不做逐句翻译，不罗列无解释的公式

**章节组织**：
- 不严格遵循原书章节编号，按用户规划命名
- 章节顺序由用户逐步添加

**覆盖范围**：
- ✅ 教材正文
- ✅ 例题
- ❌ 课后习题（目前不覆盖）

**当前进度**：尚未开始

---

### Hennessy & Patterson — 《计算机体系结构：量化研究方法》（第 6 版，2019）

**子文件夹**：`Hennessy-计算机体系结构量化方法/`

**笔记定位**：第 1 次学习 —— 体系结构高级纵深，Patterson COD 的"研究生级"进阶。以量化设计方法论为纲，每个设计决策用数据和公式评估（Amdahl's Law、性能方程、能耗方程）。需要 COD 的基础概念作为前提。

**教材位置**：`E:\study_Conputer Architechture(Quantitive)\ai-reading\textbook-md\`

教材为单个 markdown 文件：
- `Computer-Architecture-6th-Edition.md`

配套图片位于 `images/` 文件夹。

**教材特点**：Hennessy & Patterson 的"另一本 P&H"——如果说 COD 是"computer organization"，CAQA 就是真正意义上的"computer architecture"。覆盖量化设计基础、存储器层次结构优化、指令级并行（动态调度/分支预测/猜测执行/多发射）、数据级并行（向量/SIMD/GPU）、仓库级计算、领域专用架构。强调"Make the common case fast"与 Amdahl's Law 贯穿全书。

**与 Patterson COD 的关系**：
- COD 的流水线、Cache 基础 → CAQA 在此之上深入优化策略和量化折中
- COD 的 I/O、多核初步 → CAQA 展开到 WSC、DSA、互连网络
- 笔记中遇到 COD 已覆盖的基础概念，用 `[[Patterson-计算机组成与设计/...]]` 链接而非重复展开

**笔记风格**（与 COD 一致，差异在深度）：
- 推导：关键步骤与思想方法写出，不展开全部代数运算（同通用标准）
- 物理直觉与数学并重（同通用标准）
- **初学特有**：每个新概念交代"为什么需要它"的背景
- **初学特有**：数值实例充分展开，给出物理量级感
- **CAQA 特有**：量化分析是灵魂——每个结论用数字说话（"分支预测准确率从 90% → 98%，CPI 降低 0.3"），不只定性描述
- ❌ 不做逐句翻译，不罗列无解释的公式

**章节组织**：
- 不严格遵循原书章节编号，按用户规划命名
- 章节顺序由用户逐步添加

**覆盖范围**：
- ✅ 教材正文
- ✅ 例题
- ❌ 课后习题（目前不覆盖）

**当前进度**：尚未开始

---

## 论文与独立资料

**子文件夹**：`Papers/`

论文笔记格式与教材笔记一致（通用标准适用），但标题层级可更灵活——论文通常不是按章节编号的结构。

每篇论文一篇笔记，文件名：`作者-年份-主题.md`。

AI 撰写论文笔记时：
- 在 `[[知识导图]]` 的 Markmap 中找到该论文涉及的概念，将论文链接添加到对应 `####` 概念下的 L4 列表
- 标注论文的独特贡献或视角
